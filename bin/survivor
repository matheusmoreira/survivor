#!/usr/bin/env ruby

require 'survivor'
require 'survivor/options'
require 'survivor/options/parser'

options = Survivor::Options.new
Survivor::Options::Parser.new(options).parse!

Survivor.run(options)
